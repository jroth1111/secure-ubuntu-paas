#!/usr/bin/env bash
set -Eeuo pipefail

# Placeholder hook for dFlow worker resource policy. The controller owns the
# concrete app policy; this hook is intentionally permissive until policy is
# supplied by the controller.
exit 0
